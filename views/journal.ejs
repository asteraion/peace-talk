<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;600&display=swap" rel="stylesheet">
</head>
<body>
    <div class="container">
        <header>
            <a href="/" class="back-link">← Back to Tool</a>
            <h1>My Journal</h1>
            <p>Your journey to peace.</p>
        </header>

        <main>
            <% if (entries.length === 0) { %>
                <div class="card">
                    <p>No entries yet. Start by translating a thought!</p>
                    <a href="/" class="btn-primary">New Translation</a>
                </div>
            <% } else { %>
                <% entries.forEach(function(entry) { %>
                    <section class="card journal-entry fade-in">
                        <div class="meta">
                            <span class="date"><%= new Date(entry.timestamp).toLocaleDateString() %></span>
                        </div>
                        <div class="original">
                            <strong>Thought:</strong>
                            <p>"<%= entry.thought %>"</p>
                        </div>
                        <div class="translation">
                            <strong>NVC Translation:</strong>
                            <p style="white-space: pre-wrap;"><%= entry.translation %></p>
                        </div>
                    </section>
                <% }); %>
            <% } %>
        </main>
    </div>
</body>
</html>
